# Task Ledger

One row per requested user-facing objective. An objective stays **ACTIVE** until
it is **DONE** or the Boss explicitly **PARKED** it. Close the row with the live
result and the local commit.

| # | Objective | State | DONE condition | Result / commit |
|---|---|---|---|---|
| 1 | BLACK-BOXX false all-check failure | **DONE** 2026-08-06 | Runbook distinguishes shared prerequisite/executor failure from genuine subsystem failure; one root cause, not twelve | 18→17 checks green; `8355d21` |
| 2 | BLACK-BOXX boot-race ownership | **DONE** 2026-08-06 | `wg-quick@wg-ap` disabled via Loki's approval gate, `black-boxx-ap.service` sole boot owner of `wg-ap`, AP healthy, no reboot | draft `dr_c939bae02949` → incident `hi_66777414c2b0` repaired; `42380d1` |
| 3 | Antigravity CLI install + durable AI context migration | **DONE** 2026-08-06 | agy installed and authenticated, durable model-independent context + rules + skills + Loki Builder exist, agy answers the handoff question correctly from project context, no production behaviour changed, committed locally | agy 1.1.10 authenticated; workspace context, 10 skills and the Loki Builder role verified live via read-only `agy -p`; see `ANTIGRAVITY_BOOTSTRAP.md` |
| 4 | Hermes/OpenRouter credit drain | **DONE** 2026-08-05 | Circuit breaker + budgets make drain impossible even if dedup breaks | `51fda47`, 30 tests |
| 5 | Maintenance incident amplification / Telegram spam | **DONE** 2026-08-05 | Persistent dedupe, one Hermes job per unresolved incident, Discord ops feed | `daf150e`, 17+22 tests |
| 6 | Tracearr update path | **DONE** 2026-08-05 | Approval-gated, pinned, backed up, verified, rollback-capable; v1.5.0 live | `b075780`, `251807b` |
| 7 | Joplin note read-back | **DONE** 2026-08-05 | Loki can read back notes it writes to the Boss's own notebooks | `dc479a6` |
| 8 | Presence notification style | **DONE** 2026-08-05 | Concise legacy style; roommate state retained on welcome-home | `ede172d`, 20 tests |
| 9 | Google Sheets export | **DONE** 2026-08-06 | Reliable sync queue, no data loss on failure, idempotency maintained | Durable queue reading `sheets_ok=0` from SQLite |
| 10 | qBittorrent recurring connectivity | **DONE** 2026-08-06 | Root cause proven, durable fix applied, LAN+proxy+nzb360 stable beyond recurrence window | Removed `mem_limit: 1g` from compose; `qbittorrent_health` runbook added |
| 11 | RAZR disk growth / Phase-1 capacity recovery | **DONE** 2026-08-08 | Root at 78%→29%; LV extended online (100→235 GiB), orphan blob removed, caches cleaned | `lvextend`+`resize2fs` online; no downtime |
| 12 | Filebrowser production failure | **DONE** 2026-08-09 | Root cause proven, durable repair, container up, storage accessible, HTTP/LAN/proxy verified, survives recreate, Loki sees it healthy, 8-point Reliability penalty clears naturally | Stale FUSE endpoint at `/mnt/unicron-downloads`, not a path conflict; `ExecStartPre` unmount + MagicDNS + `Before=docker.service` + backoff; `rslave` binds; `filebrowser_health` runbook, 23 tests. Reliability 87→95 |
| 13 | Unicron sshfs share restoration | **DONE** 2026-08-09 | SSH auth restored, `/srv/unicron` populated, Filebrowser browses it, systemd mount persistent, no stale FUSE loop, no unrelated service damaged | Two faults: dex247's key missing from the rebuilt box **and** `/dev/sdb1` dropped from asus's fstab so `/mnt/Disk1/downloads` did not exist. Key installed via razr's pre-existing access; disk pinned by UUID with `nofail`; host key verified 3 ways and pinned, unit tightened to `StrictHostKeyChecking=yes`. 53 entries live |
| 14 | asus fstab CIFS parse errors | **DONE** 2026-08-09 | Both malformed entries corrected, fstab parses cleanly, both CIFS shares mount, existing storage/Filebrowser paths unaffected | Unescaped spaces in the source field meant systemd generated **no unit at all** for `Zion Cinema`/`Folder 1`; now `\040`-escaped. 0 parse errors (was 2); both mounted and readable; 3 pre-existing CIFS mounts, `/mnt/Disk1` and Filebrowser all undisturbed |
| 15 | Nextcloud private-download public links | **DONE** 2026-08-09, live | External recipient gets a working public HTTPS share URL, no LAN/VPN needed, Keep/Delete work end-to-end, internal addresses never exposed | Two root causes: URL built from the internal base **and** `nextcloud_integration` imported before `load_dotenv()`, pinning it to the unreachable pre-rebuild asus fallback. Token now taken from OCS and re-based onto `NEXTCLOUD_PUBLIC_BASE_URL`; per-batch folders; read-only shares, 72 h default expiry. Verified live: anon GET 200, revoked link 404. 32 tests |
| 16 | Tracearr registry version drift | **DONE** 2026-08-09, corrected 2026-08-10 | Registry matches live production, Tracearr untouched, update-workflow version comparison verified correct against a v2.x installed version | Live v2.0.1 confirmed independently via dispatcher before any edit (not trusted from the pre-existing uncommitted diff). Registry `version`/`image_digest` confirmed correct; fixed a separate swapped-IP bug in `dependencies.*.container_ip`; addendum on stale restart-churn evidence. 4 new tests, 110 total in `tests/test_nas_tracearr.py`. **Corrected 2026-08-10:** the v1.5.0→v2.0.1 update was Loki's own approval-gated `tracearr_update`, not watchtower — Loki's Joplin maintenance log has its own success record (`prepare_id e7f4e2ef6f381073`) matching the container recreate timestamp to the minute. `applied_by` field fixed to `loki_approval_gate`. See `CURRENT_HANDOFF.md`. |
| 17 | Hermes resilient diagnostic capability (provider fallback) | **DONE** 2026-08-10, live on razr + dex247 | Cost-tiered fallback chain live and verified; per-provider-aware status vocabulary; `paused_auth` gap fixed; no bridge changes without sign-off; no Anthropic credential; no paid Hermes request anywhere | OpenRouter confirmed exhausted (402, read-only). Fable (Claude Fable 5) investigated twice — native Anthropic path withdrawn (Boss has no Anthropic account), then found genuinely reachable via the existing OpenRouter key but with only $0.36 left on a $10 cap — explicitly rejected by the Boss on cost grounds, not configured anywhere. Final chain, verified via Hermes Agent's own CLI: primary `anthropic/claude-sonnet-5` (openrouter) → fallback 1 `gemma4-12b-balanced:latest` (local Ollama on razr, custom provider, $0) → fallback 2 `deepseek/deepseek-v4-flash-0731` (openrouter, $0.08/$0.18 per M, reuses the existing key) — no frontier model automatic. Configured via a direct, schema-verified edit to `/home/hermes/.hermes/config.yaml` (Hermes Agent's own config, no CLI flag path exists for a fresh list key, interactive pickers refuse to run outside a real terminal). Also fixed a real dex247 gap: `paused_auth` bridge job state was never fed to `hermes_guard.py`, so a bad credential never opened the circuit. Added `auth` as its own failure class (opens instantly like billing) and a `status_label` vocabulary. Flagged that the bridge's own cost accounting only prices the two OpenRouter-routed Anthropic models and would show $0 for a job served by either configured fallback — `last_serving_model_cost_telemetry` surfaces that rather than agreeing. Bridge (`~/hermes-bridge` on razr) deliberately not touched — Boss declined that scope. 15 new tests, `tests/test_hermes_guard.py` now 45. See `.agents/skills/hermes-operations/SKILL.md`. |

| 18 | Roommate presence wording cleanup | **DONE** 2026-08-10 | "Rob stepped out." when Boss is home and Rob leaves, exactly; no redundant restatement of Boss's own presence; existing preferred phrases (office check-in/out, leave/welcome-home) untouched; no notification volume increase; tests pass | Rob's arrival/departure wasn't pre-voiced by HA like the Boss's four transitions — a plain factual HA message was still Groq-rewritten into "Boss, your roommate has left the premises while you are still at home." `personality.py` gained `ROOMMATE_PRESENCE` (regex-matched on the roommate's name, since HA's exact wording for this event isn't fixed) + `roommate_presence_text()`, which answers from Rob's live state, never from parsing HA's phrasing. Bypasses the rewriter the same way the Boss's four already do. 12 new tests, `tests/test_presence_notifications.py` now 32. |
| 19 | Generic ("a person") presence wording fix | **DONE** 2026-08-10 | A known Rob presence event never says "a person"; Rob named directly; existing preferred phrases untouched; no notification volume increase; tests pass | Root cause: unnamed HA presence messages ("Boss, a person has been detected at home.") had no name to bypass on (row 18's fix only matched named messages) *and* the LLM fallback's presence context only ever queried `person.kavaris`, never Rob — two real bugs, both fixed. New `personality.GENERIC_PRESENCE` matches generic phrasing + a presence-shaped word (so a doorbell "someone" isn't swept in); `ha_integration._resolve_generic_presence()` resolves WHO by diffing each resident's live state against a new `presence_monitor.last_known()` accessor (presence_monitor's own detection logic untouched) rather than guessing from wording. Ambiguous/unreachable cases relay HA's raw text instead of misattributing. 12 new tests, `tests/test_presence_notifications.py` now 48, including a direct regression pin on the exact reported message. |
| 20 | Video-doorbell announcement reliability | **DONE** 2026-08-10, Boss confirmed audible in person | Root cause proven (not assumed); repair validated by the Boss physically hearing the real announcement, not by state/log evidence alone; no unrelated automation/entity changed; presence condition added; failure diagnostics added | Closed in two passes — see `COMPLETED_WORK.md` for full detail. **Pass 1** (looked complete, wasn't): fixed a hardcoded dead chime URL (`192.168.1.247`, decommissioned host) by switching to `media-source://media_source/local/doorbell.mp3`; added the missing `condition: state person.kavaris == home`; added diagnostic automation `automation.doorbell_sensor_state_diagnostics`. Validated via a real `POST /api/states/...` state-change event — trigger, condition, script, and `media_player.clock` all transitioned correctly, zero errors. Reported as fixed. **It wasn't** — the Boss listened in person and heard nothing; that live test is what actually caught the remaining bug, since no HA state/log signal could reveal it. **Pass 2** (real root cause): `media-source://` resolution and `tts.speak`'s `tts_proxy` URL are *both* built from `hass.config.internal_url`, which was also still `192.168.1.247` — confirmed via an explicit `homeassistant.components.cast.media_player` ERROR log line ("Failed to cast media ... from internal_url") on every real attempt. A literal correct URL and a fully external test URL both played back with clean Cast telemetry, proving the device/network were never the problem. REST has no way to change `internal_url`; used the WebSocket API's `config/core/update` to correct it to `192.168.1.63:8123`. Also caught live: the clock's daily-use volume (33%, fine for the voice commands the Boss uses it for constantly) wasn't reliably audible for a one-shot announcement — script now snapshots/boosts/restores volume around the announcement. And: a naive `delay: 1.5s` between two repeated TTS calls (added at the Boss's request) mostly overlapped the first message's own ~1.5s playback time since `tts.speak` returns on dispatch, not completion — fixed with a `wait_template` on the speaker returning to `idle` before starting the gap. Confirmed no unrelated automation/entity touched throughout: 434 entities / 31 automations unchanged across every step. Config-only (HA REST + WebSocket API, no restart); repo change is docs-only. Not fully ruled out: the Tapo `binary_sensor` (device_class `sound`) is a general sound-classifier, not a dedicated button-press signal — a very quiet real press could still be missed; the diagnostic automation makes this observable going forward. |
| 21 | Home Assistant camera outage (Front Door / Entryway / Living Room) | **DONE** 2026-09-02, all three cards live | Root cause proven read-only before any change; cameras restored as genuinely-added entities (not `restored` registry stubs); no unrelated service restarted; MQTT exposure not widened | Two independent faults. **(1) MQTT dead** — `mosquitto.conf` was a 1-byte file, so `eclipse-mosquitto:2.0` applied its 2.0 defaults (loopback-only listener inside the container, `allow_anonymous` false) and locked out both HA (`Errno 111`) and Frigate (`MQTT disconnected` every 2 min). No prior config was recoverable anywhere, so it was **reconstructed**, not restored; container recreated via compose `--no-deps`. Published port hardened dual-stack → **IPv4-only** (`"1883:1883"` → `"0.0.0.0:1883:1883"`) because the NAS has globally-routable IPv6 and docker-proxy was publishing on `[::]` — an anonymous broker would have been WAN-reachable over v6; edge filtering could not be verified from inside the LAN so it was not assumed. **`[::]:1883` count now 0.** MQTT is **anonymous as a TEMPORARY compatibility configuration** — auth needs credentials in Frigate's `config.yml` plus a Frigate restart, out of scope; migration is now top of the security queue in `CURRENT_HANDOFF.md`. **(2) Integration incompatible** — Frigate HA integration v5.15.4 passes deprecated `via_device`, which HA **2026.9.0** now raises on, so `camera.front_door` and `camera.livingroom` were never added (2 RuntimeErrors/boot, matching the 2 `restored: true` stubs). HACS **v5.15.5** fixes exactly this (release #1116, verified against upstream source before installing — 21 hardcoded call sites replaced by a `via_device_id` branching helper). Installed via `update.install`; a config-entry reload was deliberately refused (stale `sys.modules` would re-run the old code); HA restart approved and taken. Result: **48/48 Frigate entities available**, all three cameras `recording` with `restored=None`, zero `via_device` errors. **Upstream packaging quirk:** v5.15.5 ships `manifest.json` still declaring `5.15.4`, so HACS will keep offering the update forever — cosmetic, do **not** reinstall repeatedly. **Entryway self-recovered** to ~5 fps at 22:22:02 after ~2.5 h of ffmpeg/go2rtc failure; **root cause never established**, and the duplicate Tapo entity `camera.entryway_door_hd_stream` went unavailable right after (`Operation not permitted` on direct RTSP) — concurrent-session limit is a **hypothesis, not confirmed**. Loki could not restart the broker itself (no docker group, no NOPASSWD sudo, no dispatcher verb); both privileged steps were handed to the Boss. Rollback backup retained at `/volume1/docker/.loki-backups/frigate-hass-integration-2026-09-02/`. Docs-only repo change. |

## Backlog — not started, not authorized

| Objective | State | Note |
|---|---|---|
| Weekly Discord export 403 | UNFINISHED | Needs a Boss-side Discord permission, not code |
| watchtower → monitor-only | UNFINISHED | Production change, needs approval. The NAS watchtower's actual reach is unconfirmed — the one case it was blamed for (Tracearr v2.0.1) turned out to be Loki's own approval-gated update (corrected 2026-08-10, see #16). dex247's watchtower auto-updating every container daily is the still-real, separately-confirmed case (see `watchtower-autoupdates-conflict` memory) |
| Tracearr restart churn | UNFINISHED | 268 restarts, cause unproven, no auto-repair by design |
| Tracearr v2.x compatibility review | UNFINISHED | v2.0.1 already live (applied via Loki's own approval-gated update 2026-08-07, not watchtower — corrected 2026-08-10; registry reconciled #16); a deliberate feature/compatibility review is separate and still not scheduled |
| Telegram voice messages | UNFINISHED | `_handle()` reads text/caption only |
| Centralized Model Router | PLANNED | Design only. Requires explicit approval to start |
| `/home/unimatrix_001` mode 0777 | UNFINISHED | NAS security issue, needs tested remediation |
| Bridge-side cost accounting for non-OpenRouter providers | UNFINISHED | `~/hermes-bridge/lib/budget.mjs` rate card + `lib/usage.mjs` spend probe only cover the two OpenRouter-routed Anthropic models; a job served by either configured Hermes fallback (local Ollama or DeepSeek) prices at $0 in the bridge's own ledger — harmless for Ollama (genuinely free), silently wrong for DeepSeek (real nonzero cost hidden). Needs a razr-side bridge change; explicitly declined in scope for #17 (second production service, second machine) |

## How to use this

When the Boss assigns work, add a row with the DONE condition **stated before
you start**. Update it when the objective closes. Do not add rows for
speculative work, and do not start a backlog item because it looks easy — the
backlog is not an authorization.
